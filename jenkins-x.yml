buildPack: python37
pipelineConfig:
  pipelines:
    pullRequest:
      preBuild:
        steps:
        - command: python3 -V && python3 -m pip install -U pip && python3 -m pip install coverage && python3 app/manage.py test server.tests
          name: install-run-cov
