buildPack: python
pipelineConfig:
  pipelines:
    pullRequest:
      preBuild:
        steps:
        - command: pip install coverage && pip freeze && coverage run --source=app app/manage.py test server.tests
          name: install-run-cov
