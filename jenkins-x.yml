buildPack: python
pipelineConfig:
  pipelines:
    pullRequest:
      preBuild:
        steps:
        - command: pwd && ls -la && cd /workspace/source && ls -la && pip install -U pip && pip install coverage && pip freeze && coverage run --source=app app/manage.py test server.tests
          name: install-run-cov
