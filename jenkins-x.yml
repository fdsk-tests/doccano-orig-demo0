buildPack: python
pipelineConfig:
  pipelines:
    pullRequest:
      build:
        preSteps:
        - command: coverage run --source=app app/manage.py test server.tests
          name: doccano-py-test
